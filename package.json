{
  "name": "drew",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "nodemon --watch src --ext js,html,css --exec electron src/main.js"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "electron": "^32.2.0",
    "electron-reload": "^2.0.0-alpha.1",
    "nodemon": "^3.1.10"
  }
}
